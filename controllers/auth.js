export const email_login = (req, res) => {
  res.redirect('/')
}

export const register_email = (req, res) => {
  res.redirect('/')
}

export const discord_login = (req, res) => {
  res.redirect('/')
}

export const discord_callback = (req, res) => {
  res.redirect('/')
}

export const logout = (req, res) => {
  res.redirect('/auth/login')
}
